---
"makeanagent": minor
---

Initial base image release. Includes pre-built frontend, backend, mcp-server, Caddy reverse proxy, and supervisor. Uses @makeanagent/ package scope. Per-project deployments layer only packages/functions/src on top.
